{
  "name": "castmd",
  "private": true,
  "version": "1.3.0",
  "description": "Chrome extension — auto-tests for pure lib modules. The extension itself has no build step; this package.json exists only to run node:test against lib/.",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/*.test.js'"
  },
  "devDependencies": {
    "linkedom": "^0.18.5"
  }
}
